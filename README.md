# miniOS

A very simple Operating System, kernel and boot loader.

Coded all the way from the kernel starting in real mode, to running 4 tasks concurrently.

Qemu and gdb required for the makefile.

```make gdb``` to compile and run
